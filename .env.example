# Copier vers .env à la racine du projet (non versionné)

DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=Urbyn <onboarding@resend.dev>

# Admin (interface /admin/login)
ADMIN_ID=
ADMIN_PWD=

# Optionnel
APP_ENV=development
